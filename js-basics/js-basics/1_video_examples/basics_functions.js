//Performing a task finction
function greet () {
   console.log('Hello World');
}
greet(); //этой коиандой мы вызываем функцию call function

function hello (name, alias) {
    console.log('Hello ' + name + ' ' + alias);
}
hello('Vova', '40')


//types of functions 

// Calculating a value function
function square (number) {
    return number * number;
}
console.log(square(2))
let number = square(4);
console.log(number);


